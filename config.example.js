// Copy this file to config.js and fill in your values.
// config.js is gitignored — never commit real keys.
//
// Get your Firebase config from:
// console.firebase.google.com → your project → Project Settings → Your apps → Web app → SDK setup
// Copy the firebaseConfig object values below.

const FIREBASE_CONFIG = {
  apiKey:            "YOUR_API_KEY",
  authDomain:        "YOUR_PROJECT_ID.firebaseapp.com",
  projectId:         "YOUR_PROJECT_ID",
  storageBucket:     "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId:             "YOUR_APP_ID"
};

// App salt — change this to any random string (just don't change it after setup)
const APP_SALT = 'pawcals-v1-2026';
